spring:
  application:
    name: point-api
  profiles:
    include:
      - db
      - server-port
      - aws-sqs
    group:
      local: db-local,port-local,aws-sqs-producer-local
      test: db-test
    active: local
